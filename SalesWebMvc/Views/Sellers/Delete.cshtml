@model SalesWebMvc.Models.Seller

@{
    ViewData["Title"] = "Delete";
}

<h2>@ViewData["Title"]</h2>

<h3>Are you sure you want to delete this?</h3>

<h4>Seller</h4>
<hr />

<div>
    <dl class="dl-horizontal">
        <dt>@Html.DisplayNameFor(seller => seller.Name)</dt>
        <dd>@Html.DisplayFor(seller => seller.Name)</dd>

        <dt>@Html.DisplayNameFor(seller => seller.Email)</dt>
        <dd>@Html.DisplayFor(seller => seller.Email)</dd>

        <dt>@Html.DisplayNameFor(seller => seller.BirthDate)</dt>
        <dd>@Html.DisplayFor(seller => seller.BirthDate)</dd>

        <dt>@Html.DisplayNameFor(seller => seller.BaseSalary)</dt>
        <dd>@Html.DisplayFor(seller => seller.BaseSalary)</dd>

        <dt>@Html.DisplayNameFor(seller => seller.Department)</dt>
        <dd>@Html.DisplayFor(seller => seller.Department.Name)</dd>
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Index">Back to list</a>
    </form>
</div>
