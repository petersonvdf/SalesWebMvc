@model SalesWebMvc.Models.SalesRecord

@{
    ViewData["Title"] = "Delete";
}

<h2>@ViewData["Title"]</h2>

<h3>Are you sure you want to delete this?</h3>

<h4>Sales record</h4>
<hr />

<div>
    <dl class="dl-horizontal">
        <dt>@Html.DisplayNameFor(sale => sale.Date)</dt>
        <dd>@Html.DisplayFor(sale => sale.Date)</dd>

        <dt>@Html.DisplayNameFor(sale => sale.Amount)</dt>
        <dd>@Html.DisplayFor(sale => sale.Amount)</dd>

        <dt>@Html.DisplayNameFor(sale => sale.Seller)</dt>
        <dd>@Html.DisplayFor(sale => sale.Seller.Name)</dd>

        <dt>@Html.DisplayNameFor(sale => sale.Seller.Department)</dt>
        <dd>@Html.DisplayFor(sale => sale.Seller.Department.Name)</dd>

        <dt>@Html.DisplayNameFor(sale => sale.Status)</dt>
        <dd>@Html.DisplayFor(sale => sale.Status)</dd>
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Index">Back to list</a>
    </form>
</div>
